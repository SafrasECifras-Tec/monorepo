auto-install-peers=true

# Next.js 15 requires hoisted packages in pnpm monorepos.
# Without this, RSC manifest lookups fail with
# "Could not find module ... in the React Server Consumer Manifest"
public-hoist-pattern[]=*
