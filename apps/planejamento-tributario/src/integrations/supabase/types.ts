// Shim — re-exports from @socios/database
// The canonical Database type now lives in packages/database.
// Regenerate: pnpm supabase gen types typescript --project-id <id> > packages/database/src/supabase.types.ts
export type { Json, Database } from "@socios/database";
